# Names only. Real values live in env/enc/*.env.enc (sops + age).
# Activate with: just env-use dev
APP_ENV=
LOG_LEVEL=
# Public build-time origin only; never place credentials in PUBLIC_* variables.
PUBLIC_CLARITAS_API_BASE_URL=
